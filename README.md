# GZHU Seat Booking

GZHU Seat Booking（Android 主工程 + 认证研究脚本）。

## 仓库结构

- `android-app/`：Android 客户端（Kotlin + Jetpack Compose）
- `android-app/docs/`：技术文档（架构、接口、认证、运维）
- `CODE/`：认证链路研究脚本与脱敏分析材料

## 使用教程

### 1. 应用权限与系统设置

首次安装后，建议完成以下设置以保证定时任务稳定：

1. 允许通知权限（用于执行结果提醒与日志导出通知）
2. 允许后台运行、关闭电池优化限制
3. 允许自启动与后台保活（不同厂商系统入口不同）

### 2. 登录并获取会话

1. 在`配置`页输入“统一身份认证账号/密码”
2. 点击`登录（获取会话）`
3. 当“会话状态”显示“已获取登录会话，可直接请求接口”表示成功
4. 通过 `Room`、`座位`选择目标自习室和座位
5. 点击右上角 `‼️教程‼️` 可随时查看完整操作步骤

### 3. 设置自动预约任务

1. 在“每周预约时段”中先填好并启用需要的时间段
2. 打开 `启动每日预约任务`
3. 设置 `启动时间(HH:mm)`，例如 `07:15`

默认新增模板：

1. `09:00-13:00`
2. `13:00-16:00`
3. `16:00-20:00`
4. `20:00-22:15`
5. 第 5 条及之后为空时间段

### 4. 在监测页核对任务状态

进入 `监测` 页后重点检查：

1. `每日预约任务已在系统启用`
2. `Alarm / Work / Job` 三通道均为绿色
3. 计划触发时间是否符合预期
4. 每周状态颜色是否正常（灰=关闭，绿=正常，红=配置异常）
5. 若系统提前拉起定时服务，任务会自动等待到计划触发时间再执行

### 5. 结果与日志

1. `监测`页查看成功记录/失败记录
2. `日志`页可导出 ZIP 与清空日志
3. 遇到 `Unable to resolve host` 会自动进入 `5s一次，最多5min` 重试，恢复后继续原业务
4. “请求频繁但最终成功”的时段不会进入失败记录区，但会保留完整日志

## 界面示意图

### APP首页

![APP首页](wiki/images/APP首页.jpg)

用于登录获取会话、设置触发时间、开关自动预约，以及选择房间与座位。


### 座位配置

![座位配置](wiki/images/座位配置图.jpg)

用于查看今日与明日占用情况，并配置每周预约时段。

### 房间树示意

![房间树示意](wiki/images/广大图书馆房间树示意图.jpg)

用于展示图书馆房间层级与可选目标区域，便于快速定位预约房间。

### 监测页面

![监测页面](wiki/images/监测页面示意图.jpg)

用于核对 `Alarm / Work / Job` 三通道状态、计划触发时间、每周配置校验和执行记录。

### 日志页面

![日志页面](wiki/images/日志页面图.jpg)

用于导出日志 ZIP、清空日志，并辅助定位运行异常。

## 文档导航

- Android 总览：`android-app/README.md`
- 架构设计：`android-app/docs/ARCHITECTURE.md`
- 接口参考：`android-app/docs/API_REFERENCE.md`
- 通讯认证：`android-app/docs/AUTH_COMMUNICATION.md`
- 运维发布：`android-app/docs/OPERATIONS.md`


